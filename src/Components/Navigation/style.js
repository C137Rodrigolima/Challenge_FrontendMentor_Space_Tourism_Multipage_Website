import styled from "styled-components";

const BoxNavigation = styled.div `
    height: 96px;
    margin-top: 40px;
    display: flex;
    justify-content: space-between;
`;

const Nav = styled.div `
    width: 830px;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 48px;
`;

export default BoxNavigation && Nav;